# Shared runtime (voice pipeline, MCP client/tool-servers, dashboard) —
# see runtime/requirements.txt (the pnd-mcp submodule).
-r runtime/requirements.txt

# Persona-repo-only deps
psutil>=6.0.0  # `dann stop`/`dann restart` (dann.py) — cross-platform process discovery to reliably kill dann dev/electron/start and any orphaned API/UI children
