pvporcupine>=4.0.0
openwakeword>=0.6.0
sounddevice==0.4.6
numpy==2.5.2  # bumped from 1.26.4: onnx (needed to export trained wake-word models) requires numpy 2.x; verified faster-whisper/sounddevice/soundfile/onnxruntime/openwakeword/piper/pvporcupine all still import fine
soundfile>=0.12.1
faster-whisper>=1.0.0
requests>=2.28.0
PyYAML>=6.0
piper-tts>=1.4.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.0.0
httpx>=0.27.0
mcp==1.29.1  # pinned: 2.x dropped mcp.server.fastmcp.FastMCP, which this codebase's MCP servers use
ptyprocess>=0.7.0
anthropic>=0.40.0
aiofiles>=23.0.0
PyJWT[crypto]>=2.9.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
torch>=2.0.0  # scripts/wakeword/train_wakeword.py — trains the custom openWakeWord "ok Dann" model
onnx>=1.22.0  # scripts/wakeword/train_wakeword.py — torch.onnx.export(..., dynamo=False) needs this to serialize
pytest>=8.0.0  # tests/ — run via `.venv/bin/python -m pytest`
psutil>=6.0.0  # `dann stop` — cross-platform process discovery to reliably kill dann dev/electron/start and any orphaned API/UI children
