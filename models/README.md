Place your wake word model here.

- Expected file name (default): `ok_dann.tflite`
- Source: train with openWakeWord (see `wake-word.md`) or adapt an existing model.
- Keep large binaries out of git unless desired; add to `.gitignore` if needed.
